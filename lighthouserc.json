{
  "_comment": [
    "Desktop-profile Lighthouse CI assertions for the public marketing site.",
    "Mobile-profile assertions live in lighthouserc.mobile.json (task #248);",
    "the CI workflow runs both configs and merges the results into the PR",
    "summary. Desktop is the merge-blocking gate today; mobile starts at",
    "'warn' until the first clean rollout.",
    "All listed routes gate at 'error' for performance, accessibility,",
    "best-practices, and SEO categories AND for the LCP/CLS/TBT metric",
    "budgets, so any regression below the configured budgets blocks PR",
    "merge. Do NOT add new routes here at 'warn' without a tracked",
    "cleanup task — perf/SEO debt should be fixed at the page level,",
    "not papered over in this config."
  ],
  "ci": {
    "collect": {
      "url": [
        "http://localhost:5000/",
        "http://localhost:5000/about",
        "http://localhost:5000/services",
        "http://localhost:5000/insights",
        "http://localhost:5000/contact",
        "http://localhost:5000/library",
        "http://localhost:5000/applications"
      ],
      "numberOfRuns": 1,
      "settings": {
        "preset": "desktop",
        "skipAudits": [
          "uses-http2",
          "is-on-https"
        ]
      }
    },
    "assert": {
      "assertions": {
        "categories:performance": ["error", { "minScore": 0.85 }],
        "categories:accessibility": ["error", { "minScore": 0.9 }],
        "categories:best-practices": ["error", { "minScore": 0.9 }],
        "categories:seo": ["error", { "minScore": 0.9 }],
        "largest-contentful-paint": ["error", { "maxNumericValue": 2500 }],
        "cumulative-layout-shift": ["error", { "maxNumericValue": 0.1 }],
        "total-blocking-time": ["error", { "maxNumericValue": 300 }]
      }
    },
    "upload": {
      "target": "temporary-public-storage"
    }
  }
}
